const MOON = '🌙';
const SUN = '☀️';
const DARK_MODE = 'dark';
const LIGHT_MODE = 'light';
const DEFAULT_MODE = DARK_MODE;

const btn = document.querySelector('#theme-switcher');

(function init() {
    // Implement the logic for initial function
}())

function setMode(mode = DEFAULT_MODE) {
    // Implement the logic for setting the mode and switching between them
}

btn.addEventListener('click', function () {
    // Implement the logic for switching the mode on button click
});